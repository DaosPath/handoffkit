# Deprecated source path

Web explore/search/scrape now lives under `src/browser/` and `include/handoffkit/browser/`.

The library target is `handoffkit_browser` (`handoffkit::browser`).
Headers under `include/handoffkit/explore/` are compatibility aliases only.
