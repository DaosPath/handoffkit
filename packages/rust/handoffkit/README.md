# handoffkit

Convenience facade re-exporting `handoffkit-contracts` and
`handoffkit-protocol`.

HandoffKit 1.16 provides contracts and HK-CSP protocol types. Native async
runtime and transport crates are planned for a later release.
