import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  subsets: ["latin"],
  variable: "--font-geist-sans",
});

const geistMono = Geist_Mono({
  subsets: ["latin"],
  variable: "--font-geist-mono",
});

export const metadata: Metadata = {
  title: "Handoff Kit — Structured handoffs for reliable AI systems",
  description:
    "Handoff Kit is an open-source developer tool for orchestrating AI agents with shared state, routing, replay, auditability, and reports.",
  openGraph: {
    title: "Handoff Kit",
    description:
      "Structured handoffs for reliable multi-agent AI systems. Open source. Production ready.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}
